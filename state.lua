-- storing the state of the game
-- state to be shard/accessed in all other files

return {
  paused = false,
  button_left = false,
  button_right = false,
  level_cleared = false,
  game_over = false,
}
